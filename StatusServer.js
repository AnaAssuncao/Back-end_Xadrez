const room={
    roomWithOnePlayer:"roomWithOnePlayer",
    roomUnavailable:"roomUnavailable",
    connectedRoom:"connectedRoom",
    noExistRoom:"noExistRoom",   
    reconnectRoom:"reconnectRoom",
    roomNotReconnected:"roomNotReconnected"
}

const statusGame ={
    chess:"statusGame",
    endGame:"endGame",
    playerWin:"playerWin",
    giveUp:"giveUp",
    timeOutToMove:"timeOutToMove",
    incorretMovement:"IncorretMovement"
}

const movement={
    waitAgain:"waitAgain",
    movementUnavailable:"movementUnavailable",
    correctMovement:"correctMovement",
    incorrectMovement:"incorrectMovement",
    movementAvailable:"movementAvailable",
    confirmedMove:"confirmedMove" 
}

module.exports={room,statusGame,movement}